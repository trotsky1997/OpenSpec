## MODIFIED Requirements

### Requirement: Final recap

The skill SHALL conclude with a recap and command reference.

#### Scenario: Final recap
- **WHEN** onboarding is complete
- **THEN** agent summarizes the workflow phases completed
- **AND** emphasizes this rhythm works for any size change
- **AND** provides a command reference table without `/opsx:verify`
- **AND** suggests next actions such as `/opsx:new`, `/opsx:ff`, or
  `/opsx:apply`
- **AND** treats implementation-time validation as part of apply rather than a
  separate public verify command
