## MODIFIED Requirements

### Requirement: Final recap

The skill SHALL conclude with a recap and command reference.

#### Scenario: Final recap

- **WHEN** onboarding is complete
- **THEN** agent summarizes the workflow phases completed
- **AND** emphasizes this rhythm works for any size change
- **AND** presents `SolidSpec` as the strict workflow name for git-governed,
  code-disciplined execution
- **AND** provides a command reference table that includes the existing
  `/opsx:*` workflow commands and notes that matching `/ssx:*` aliases are also
  available
- **AND** suggests next actions such as trying `/opsx:new`, `/ssx:new`,
  `/opsx:ff`, or `/ssx:ff`
